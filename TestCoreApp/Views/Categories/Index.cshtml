@model IEnumerable<Category>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">


    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Categories View</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Categories" asp-action="New" class="btn btn-primary">
                <i class="bi bi-plus"></i> &nbsp; Add New Category
            </a>
        </div>
    </div>

    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">
                    Category Name
                </th>
                <th scope="col">
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var cat in Model)
            {
                <tr class="table-primary">
                    <td scope="row" width="30%">
                        @cat.Name
                    </td>
                    <td>
                        <a asp-controller="Categories" asp-action="Edit" asp-route-Id="@cat.Id" class="btn btn-sm btn-success">
                            <i class="bi bi-pencil"></i>
                        </a>
                        &nbsp;
                        <a asp-controller="Categories" asp-action="Delete" asp-route-Id="@cat.Id" class="btn btn-sm btn-danger">
                            <i class="bi bi-trash3"></i>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

